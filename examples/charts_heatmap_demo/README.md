# Charts Heatmap Demo

这个示例提供一个最小的独立 `heatmap` 入口，用来稳定展示当前最值得推荐的几项能力：

- 行列标签
- `compact legend`
- 低色彩兼容的字符热度表达
- `RowWindow / ColWindow`
- `MaxRowLabelWidth`

运行：

```bash
go test ./examples/charts_heatmap_demo
go run ./examples/charts_heatmap_demo
```
