# Charts ScatterPlot Demo

这个示例提供一个最小的独立 `scatterplot` 入口，目标不是展示所有能力，而是给 README 和回归测试一个稳定的参考面。

当前示例覆盖：

- 双系列散点
- 显式 `Domain`
- 命名参考线
- 命名参考区间带
- `legend / grid / axis`

运行：

```bash
go test ./examples/charts_scatterplot_demo
go run ./examples/charts_scatterplot_demo
```
